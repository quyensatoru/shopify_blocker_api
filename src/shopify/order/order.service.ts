import { Injectable } from '@nestjs/common';

@Injectable()
export class OrderService {
    async getOrder(id: string) {
        try {

        } catch (e) {
            console.error(e);
        }
    }
}
